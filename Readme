To run v1 program:

install python on machine
pip3 install mysql.connector
In the init function, change the mysql connection details to point at your desired MYSQL database
Hit save
Run the program. In the terminal, run: python3 createTableV1.py


To run v2 program:

install python on machine
pip3 install mysql.connector
pip3 install faker
Add your mysql connection details in the MySQLConnection object instantiation parameters on the first line of the the main() function
Hit save
Run the program. In the terminal, run: python3 createTableV2.py

Notes:
